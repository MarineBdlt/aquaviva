# Copy to .env on the server (never commit real secrets).
SECRET_KEY=change-me-to-a-long-random-string
SESSION_COOKIE_SECURE=true

# Optional: unused by /reserver (FormSubmit delivers to site_email in CMS).
# MAIL_USERNAME=
# MAIL_PASSWORD=
